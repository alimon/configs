context:
  test_character_delay: 10

device_type: x86
job_name: lkft-libhugetlbfs-${KERNEL_BRANCH}-${BUILD_NUMBER}
timeouts:
  job:
    minutes: 60
  action:
    minutes: 10
  connection:
    minutes: 2
priority: medium
visibility: public

metadata:
  kernel-branch: ${KERNEL_BRANCH}
  kernel-repo: ${KERNEL_REPO}
  kernel-commit: ${KERNEL_COMMIT}
  kernel-describe: ${KERNEL_DESCRIBE}
  kernel-config: ${KERNEL_CONFIG_URL}
  libhugetlbfs-url: ${LIBHUGETLBFS_URL}
  libhugetlbfs-version: ${LIBHUGETLBFS_VERSION}
  libhugetlbfs-revision: ${LIBHUGETLBFS_REVISION}
  build-url: ${BUILD_URL}
  build-location: ${SNAPSHOTS_URL}/${BASE_URL}
  series: lkft

actions:
- deploy:
    timeout:
      minutes: 15
    to: tftp
    kernel:
      url: ${KERNEL_URL}
    nfsrootfs:
      url: ${NFSROOTFS_URL}
      compression: xz
    os: oe

- boot:
    timeout:
      minutes: 10
    method: ipxe
    commands: nfs
    parameters:
      shutdown-message: "reboot: Restarting system"
    auto_login:
      login_prompt: 'login:'
      username: root
      login_commands:
        - su
    prompts:
    - 'root@intel-core2-32:'

- test:
    timeout:
      minutes: 60
    definitions:
    - repository: git://git.linaro.org/qa/test-definitions.git
      from: git
      path: automated/linux/libhugetlbfs/libhugetlbfs.yaml
      name: libhugetlbfs
      timeout:
        minutes: 30
