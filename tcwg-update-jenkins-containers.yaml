# Auto generated from tcwg-update.yaml.in and tcwg-update/jenkins-containers.def. Do not edit.
- job:
    name: tcwg-update-jenkins-containers
    project-type: freestyle
    defaults: global
    properties:
        - authorization:
            anonymous:
                - job-read
                - job-extended-read
            linaro:
                - job-build
                - job-cancel
        - build-discarder:
            days-to-keep: 30
            num-to-keep: 100
        - build-blocker:
            use-build-blocker: true
            blocking-jobs:
              - "tcwg.*"
            block-level: 'NODE'
            queue-scanning: 'BUILDABLE'
        - heavy-job:
            # This is the maximum number of executors that tcwg-* nodes have.
            weight: 4
        - throttle:
            max-per-node: 1
            option: project
    parameters:
        - label:
            name: nodes
            # We can't restart coordinator nodes because we can't block-out
            # matrix master jobs with heavy-job plug-in above.
            default: tcwg && !tcwg-coordinator && !tcwg-bmk-coordinator
            all-nodes: true
            matching-label: 'allCases'
            node-eligibility: 'ignore-offline'
            description: 'Machines to run on'
        - string:
            name: distro
            default: 'xenial'
            description: 'Distro version to use.'
        - bool:
            name: force
            default: 'false'
            description: 'Whether to force update even with no changes in image'
        - bool:
            name: verbose
            default: 'true'
            description: 'Whether to be verbose'
        - string:
            name: scripts_branch
            default: master
            description: 'Scripts revision to use'
    disabled: false
    node: tcwg-coordinator
    concurrent: true
    display-name: 'TCWG CCC Update jenkins containers'
    scm:
        - git:
            url: https://git.linaro.org/toolchain/jenkins-scripts.git
            refspec: +refs/heads/*:refs/remotes/origin/* +refs/changes/*:refs/changes/*
            branches:
                - ${scripts_branch}
            skip-tag: true
            shallow-clone: true
            wipe-workspace: true
    triggers:
        - timed: '@daily'
    wrappers:
        - timeout:
            timeout: 600
        - timestamps
        - build-name:
            name: '#${BUILD_NUMBER}-${NODE_NAME}'
    builders:
        - shell: |
            #!/bin/bash
            set -ex

            ./tcwg-update-host-containers.sh \
            --distro "$distro" \
            --node "$NODE_NAME" \
            --force "$force" \
            --verbose "$verbose"
# checksum: 00d22a8ada5ca4de3448c6e4ef1b9581
